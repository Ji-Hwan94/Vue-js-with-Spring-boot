<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springbootapp.user.repository.UserRepository">

    <select id="findById" resultType="com.example.springbootapp.user.dto.UserResponseDto">
        SELECT id, username, email, created_at, updated_at
        FROM users
        WHERE id = #{id}
    </select>

    <select id="findAll" resultType="com.example.springbootapp.user.dto.UserResponseDto">
        SELECT id, username, email, created_at, updated_at
        FROM users
        ORDER BY created_at DESC
    </select>

    <select id="findByUsername" resultType="com.example.springbootapp.user.dto.UserResponseDto">
        SELECT id, username, email, created_at, updated_at
        FROM users
        WHERE username = #{username}
    </select>

    <select id="findByEmail" resultType="com.example.springbootapp.user.dto.UserResponseDto">
        SELECT id, username, email, created_at, updated_at
        FROM users
        WHERE email = #{email}
    </select>

    <insert id="insertUser" parameterType="com.example.springbootapp.user.dto.UserRequestDto">
        INSERT INTO users (username, email, password, created_at)
        VALUES (#{username}, #{email}, #{password}, now())
    </insert>
</mapper>